<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProcessPause.WPFApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,10,0,10" HorizontalAlignment="Center">
            <ComboBox x:Name="Filter_ComboBox" Width="100">
                <ComboBoxItem Content="Id" />
                <ComboBoxItem Content="Name" />
            </ComboBox>
            <TextBox x:Name="Filter_TextBox" Width="250" Margin="10,0,10,0" />
            <Button Content="Search" Width="100" Click="Search_OnClick"/>
        </StackPanel>
        <ListBox Margin="10" Grid.Row="1" x:Name="ProcessesList" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="Pause_Button" Content="Pause" Width="100" Margin="5" Click="Pause_Button_OnClick"/>
            <Button x:Name="Run_Button" Content="Run" Width="100" Margin="5" Click="Run_Button_OnClick" />
        </StackPanel>
    </Grid>
</Window>
